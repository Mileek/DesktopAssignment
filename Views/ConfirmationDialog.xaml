<Window x:Class="DesktopAssignment.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Title="Confirmation" Height="200" Width="400">
    <StackPanel Margin="20">
        <TextBlock Text="Are you sure?" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,20"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Yes" Width="75" Margin="0,0,10,0" IsDefault="True" Click="YesButton_Click"/>
            <Button Content="No" Width="75" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>