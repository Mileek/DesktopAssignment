<Window x:Class="DesktopAssignment.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopAssignment.Views"
         xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        Title="ShellView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock  Text="http://api.ipstack.com/" IsEnabled="False"></TextBlock>
            <TextBlock  Text="{Binding IpAddressOrUrl}" IsEnabled="False"></TextBlock>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.RowSpan="2">
            <TextBox Width="200" Margin="10" Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Add Geolocation" cal:Message.Attach="[Click] = [AddGeolocation]" Width="200" Margin="10"/>
            <ListBox ItemsSource="{Binding Geolocations}" DisplayMemberPath="IpAddressOrUrl" Width="200" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>