<Window x:Class="DesktopAssignment.Views.WarningDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        xmlns:cal="http://www.caliburnproject.org"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Title="Warning" Height="250" Width="400">
    <StackPanel Margin="20">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Height="120">
            <TextBlock Text="{Binding WarningMessage}" TextWrapping="Wrap" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,20"/>
        </ScrollViewer>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" IsDefault="True" cal:Message.Attach="[Click]=[Ok]"/>
        </StackPanel>
    </StackPanel>
</Window>