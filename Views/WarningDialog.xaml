<Window x:Class="DesktopAssignment.Views.WarningDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Title="Warning" Height="200" Width="400">
    <StackPanel Margin="20">
        <TextBlock x:Name="WarningTextBlock" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,20"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" IsDefault="True" Click="OkButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>